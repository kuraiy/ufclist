version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema : "schema.sql"
    gen:
      go:
        package: "database"
        out: "gen"
        overrides:
          - column: "fighters.age"
            go_type: "uint8"